[project]
name = "nexus-insight"
version = "1.0.0"
description = "Zero-Trust RAG backend — inferencia local, multi-departamento, sin APIs cloud"
requires-python = ">=3.13"

[tool.coverage.run]
omit = [
    "src/core/config.py",
    "src/core/database.py",
    "src/api/v1/documents.py",
]
